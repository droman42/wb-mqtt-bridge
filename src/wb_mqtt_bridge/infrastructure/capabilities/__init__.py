"""Device capability maps (Layer 1 of the scenario redesign).

Capability *data* lives in JSON under ``config/capabilities/`` and is loaded at
runtime; these modules are only the schema (``models``) and loader (``loader``).
See ``docs/scenarios/scenario_system_redesign.md`` §5 and §16.
"""
