# Target profile: LOCAL — locveil-bridge running on this machine.
# Override per-run, e.g.:  make mqtt TARGET=local MQTT_HOST=127.0.0.1
MQTT_HOST=localhost
MQTT_PORT=1883
API_URL=http://localhost:8000
