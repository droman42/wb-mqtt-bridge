---
phase: 01-dependency-reproducibility-hardening
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/test_dependency_pins.py
autonomous: true
requirements: [DEP-01]
tags: [dependencies, testing, supply-chain]

must_haves:
  truths:
    - "The full pytest suite stays green on amd64 after the re-pin (no behavior regression)"
    - "The Auralic (openhomedevice) driver tests pass with the re-pinned dependency"
    - "The Apple TV (pyatv) driver tests pass against pyatv 0.17.0"
    - "A pin-guard test fails the build if a moving `branch =` git source is reintroduced"
    - "A guard asserts openhomedevice stays lxml-free (ARMv7 hard constraint)"
  artifacts:
    - path: "tests/test_dependency_pins.py"
      provides: "Automated pin-guard: no moving git refs, lxml-free openhomedevice, bounds present"
      min_lines: 30
  key_links:
    - from: "tests/test_dependency_pins.py"
      to: "pyproject.toml"
      via: "reads and asserts on dependency declarations"
      pattern: "branch ="
    - from: "tests/test_dependency_pins.py"
      to: "uv.lock"
      via: "asserts openhomedevice transitive tree has no lxml"
      pattern: "lxml"
---

<objective>
Prove the re-pin from plan 01-01 introduced no regression and lock in the supply-chain
guarantees with an automated guard test.

Purpose: The phase definition of "done" is the full suite green on amd64 (225 baseline)
PLUS the two re-pinned drivers (Auralic / Apple TV) passing their mocked tests. Beyond a
one-time check, a permanent pin-guard test makes the DEP-01 guarantees regression-proof:
if anyone later reintroduces a moving `branch =` git ref or an lxml dependency on the
ARMv7-critical openhomedevice path, the suite fails.

Output: A new `tests/test_dependency_pins.py` guard test plus a green full suite and green
driver tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-reproducibility-hardening/01-CONTEXT.md
@.planning/phases/01-dependency-reproducibility-hardening/01-VALIDATION.md

<interfaces>
<!-- Verified repo facts the executor needs. No codebase exploration required. -->

Test runner: `uv run pytest` (pytest 7.x, asyncio_mode=auto, addopts="-v --tb=short").
Device-test files (verified to exist):
- Auralic: `tests/devices/test_auralic_device.py`, `tests/devices/test_auralic_update_task.py`
- Apple TV: `tests/unit/test_apple_tv_params.py`

Device-test pattern (from tests/devices/test_auralic_device.py docstring): construct the
device with a typed config, inject an AsyncMock/MagicMock for the external client
(openhome_device / pyatv), bypass setup(), drive handle_* methods directly. The Apple TV
param test does NOT import pyatv directly (mocked behavior only).

pyproject.toml post-01-01 facts to assert against:
- `[tool.uv.sources]` has openhomedevice with `rev = "6e862a..."` and NO `branch =`.
- pyatv appears as `"pyatv==0.17.0"` in `dependencies` with NO git source.

uv.lock post-01-01: openhomedevice 2.2.1 transitive deps are async-upnp-client only (no lxml).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the pin-guard test (no moving refs, lxml-free, bounds present)</name>
  <files>tests/test_dependency_pins.py</files>
  <read_first>
    - pyproject.toml (post-01-01: `dependencies` array and `[tool.uv.sources]`)
    - uv.lock (the `name = "openhomedevice"` block — confirm transitive tree)
    - tests/devices/test_auralic_device.py (for the project's test-file conventions: pytest markers, imports)
  </read_first>
  <behavior>
    - Test 1: pyproject.toml `[tool.uv.sources]` contains no `branch =` (no moving git ref) → DEP-01.
    - Test 2: pyproject.toml contains the openhomedevice immutable SHA `6e862a1022f59a21c57c501dcf040f81d12ebfaf` and contains no `postlund/pyatv` git URL (pyatv migrated to PyPI).
    - Test 3: pyproject.toml contains `pyatv==0.17.0`.
    - Test 4: the openhomedevice entry in uv.lock has no `lxml` in its dependency list and no top-level `name = "lxml"` package appears in uv.lock (ARMv7 hard constraint preserved).
    - Test 5: every direct PyPI dependency specifier (excluding the user's exact-pinned `pymotivaxmc2`/`asyncwebostv`, the git rev, and the `pyatv==` exact pin) carries an upper bound `<` → DEP-03.
  </behavior>
  <action>
    Create `tests/test_dependency_pins.py` as a pure-unit guard (mark with
    `pytest.mark.unit`). Read `pyproject.toml` and `uv.lock` from the repo root using
    tomllib (stdlib, Python 3.11+) for pyproject and plain text scanning for uv.lock; do
    NOT shell out to uv. Implement the five behaviors above as separate test functions with
    clear assertion messages naming the offending dependency. For Test 5, parse the
    `[project].dependencies` list, skip entries that are git URLs (`@ git+`), skip exact
    pins (`==`), and assert the remainder all contain `,<` (an upper bound). For Test 4,
    locate the `name = "openhomedevice"` block in uv.lock and assert no `lxml` token appears
    in its `dependencies` array, and assert the uv.lock text has no standalone
    `name = "lxml"` entry.

    Resolve the repo root relative to the test file location (parents of __file__) so the
    test is CWD-independent.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dependency_pins.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dependency_pins.py` exists with at least 5 test functions.
    - `uv run pytest tests/test_dependency_pins.py -v` exits 0 (all guard tests pass).
    - The lxml-free test (Test 4) asserts on both the openhomedevice block and the absence of a top-level lxml package.
    - The bounds test (Test 5) excludes `pymotivaxmc2`, `asyncwebostv`, the git rev, and `pyatv==0.17.0` from the bounded-requirement check.
  </acceptance_criteria>
  <done>
    A permanent guard test passes and would fail the build if a moving `branch =` ref, an
    lxml reintroduction on openhomedevice, or an unbounded direct PyPI dep is added later.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the driver tests and the full suite; confirm no regression</name>
  <files>tests/devices/test_auralic_device.py, tests/unit/test_apple_tv_params.py</files>
  <read_first>
    - tests/devices/test_auralic_device.py
    - tests/devices/test_auralic_update_task.py
    - tests/unit/test_apple_tv_params.py
    - .planning/phases/01-dependency-reproducibility-hardening/01-VALIDATION.md (sampling rate, phase gate)
  </read_first>
  <action>
    Run the two re-pinned drivers' tests first (the quick command), then the full suite (the
    phase gate). The Auralic and Apple TV tests are mocked (inject AsyncMock client, bypass
    setup()), so they verify the drivers still construct and dispatch correctly against the
    re-pinned dependencies without hardware. If the Apple TV path surfaces any pyatv 0.17.0
    import or API incompatibility (research assumption A1 is MEDIUM confidence), capture the
    exact import error and report it in the SUMMARY — do NOT silently patch the driver beyond
    what an import-compatibility fix requires; a real API break is a finding, not a
    rubber-stamp. On-hardware re-verification is explicitly OUT OF SCOPE (Phase 5).

    The full-suite baseline is 225 pass on amd64; the new guard test from Task 1 adds to
    that count, so expect 225 + (guard tests) pass / 0 fail.
  </action>
  <verify>
    <automated>uv run pytest tests/devices/test_auralic_device.py tests/devices/test_auralic_update_task.py tests/unit/test_apple_tv_params.py -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/devices/test_auralic_device.py tests/devices/test_auralic_update_task.py tests/unit/test_apple_tv_params.py -q` exits 0.
    - `uv run pytest -q` exits 0 with 0 failures (full suite green on amd64).
    - Total passing count is >= 225 (baseline) plus the new guard tests.
  </acceptance_criteria>
  <done>
    Auralic + Apple TV driver tests pass against the re-pinned dependencies, and the full
    amd64 suite is green — no behavior regression from the re-pin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future edits → pyproject.toml/uv.lock | A later change could reintroduce a moving git ref, an lxml dependency, or an unbounded major; the guard test is the boundary check |
| re-pinned library → driver code | pyatv 0.17.0 / openhomedevice SHA must remain API-compatible with the AppleTVDevice / AuralicDevice drivers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Tampering | Future regression that reintroduces a moving `branch =` ref or lxml on openhomedevice | mitigate | `tests/test_dependency_pins.py` asserts no `branch =`, immutable SHA present, lxml-free openhomedevice tree; CI fails on regression. |
| T-1-05 | Tampering | pyatv 0.17.0 API drift vs the AppleTVDevice driver (research A1 is MEDIUM confidence) | mitigate | Run the mocked Apple TV driver test against 0.17.0; a real import/API break is reported as a finding, not auto-patched. |
</threat_model>

<verification>
- `uv run pytest tests/test_dependency_pins.py -v` passes (DEP-01 guard).
- Auralic + Apple TV driver tests pass (re-pin caused no driver regression).
- Full suite green on amd64 (phase gate), >= 225 + guard tests.
</verification>

<success_criteria>
- DEP-01 is regression-proof: a moving git ref or lxml reintroduction fails the suite.
- No behavior regression: drivers and full suite green after the re-pin.
</success_criteria>

<output>
Create `.planning/phases/01-dependency-reproducibility-hardening/01-02-SUMMARY.md` when done.
</output>
